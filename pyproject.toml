[project]
name = "catatan-ftik"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.13"
dependencies = [
    "jupyterlab>=4.6.3",
    "kagglehub>=1.0.2",
    "pandas>=3.0.5",
]

[project.scripts]
catatan-ftik = "catatan_ftik:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
